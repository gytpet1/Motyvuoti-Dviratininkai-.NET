@model Motyvuoti_Dviratininkai.Models.KlientoPasiekimai

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>KlientoPasiekimai</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="KlientoPasiekimaiId" />
            <div class="form-group">
                <label asp-for="KlientasId" class="control-label"></label>
                <select asp-for="KlientasId" class="form-control" asp-items="ViewBag.KlientasId"></select>
                <span asp-validation-for="KlientasId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PasiekimasId" class="control-label"></label>
                <select asp-for="PasiekimasId" class="form-control" asp-items="ViewBag.PasiekimasId"></select>
                <span asp-validation-for="PasiekimasId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
