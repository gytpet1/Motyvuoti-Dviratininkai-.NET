@model IEnumerable<Motyvuoti_Dviratininkai.Models.Pasiekimas>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pavadinimas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kilometrai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pavadinimas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kilometrai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PasiekimasId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PasiekimasId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PasiekimasId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
